{
  "name": "rate-limiting-demo",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "server": "cd backend && bun run dev",
    "build": "cd frontend && bun run build",
    "dev:all": "bun run server & bun run dev",
    "test": "bun run backend/test-algorithms.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
