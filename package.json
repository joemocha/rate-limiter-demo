{
  "name": "rate-limiting-demo",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "dev:all": "concurrently \"cd backend && bun run dev\" \"cd frontend && bun run dev\"",
    "server": "cd backend && bun run start",
    "test": "bun run backend/test-algorithms.ts",
    "build": "cd frontend && bun run build && cd ../backend && bun build src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^8.2.2",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
